#include "main.h"
#include <stdio.h>

/**
 * _abs - computes the absolute value of an integer
 * @c: the int to print
 *
 *  Return: Always 0.
 */

int _abs(int c)
{
	if (c > 0)
	{
		return (c);
	}
	return (-c);
}
